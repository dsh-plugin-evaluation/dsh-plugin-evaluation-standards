{
  "name": "dsh-plugin-evaluation-standards",
  "private": true,
  "version": "1.0.0",
  "description": "Versioned shared standards for DSH plugin evaluation",
  "scripts": {
    "build:site": "node site/build.mjs",
    "validate": "node scripts/validate.mjs",
    "pretest": "npm --prefix ../dsh-plugin-evaluation-portable-runner run build",
    "test": "node --test"
  }
}
