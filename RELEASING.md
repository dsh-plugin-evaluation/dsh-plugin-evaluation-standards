# 发布规范

## 固定版本消费

发布使用语义化 Git tag，例如 `v1.0.0`。DSH 消费端必须固定使用 release tag 或 commit SHA；生产环境禁止跟随 `main` 分支。

每个实验记录必须保存：

- 方案 ID；
- 仓库版本（tag 或其他版本标识）；
- commit SHA；
- 完整方案快照。

这些信息共同保证历史实验可复现。

## 版本变更

- **补丁版本**（例如 `v1.0.1`）：文档更正、不会改变已发布方案或指标含义的元数据修复。
- **次版本**（例如 `v1.1.0`）：新增向后兼容的指标或方案，不改变既有已发布定义的含义。
- **主版本**（例如 `v2.0.0`）：引入不兼容的目录、schema 或评测语义；应保留旧版本供历史实验使用。

发布前应运行 `npm run validate`，并确保 tag 指向已审核的提交。
