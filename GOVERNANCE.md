# 治理规范

## 项目定位

本仓库是由社区维护的 DSH 插件评测标准库，不代表 DeepSeek、DSH 或其任何官方立场、认证或标准。

## 贡献与决策

所有变更通过 Fork + Pull Request 提交。维护者审核后决定是否合并；重大方向可先通过 Issue 讨论。维护者应以公开、可复现的理由审查和处理贡献。

## 标准演进规则

- 指标必须可由 DSH runner 执行，或在定义中明确标记为“尚未被 DSH runner 支持”。
- 未获 runner 支持的指标不得加入默认方案。
- 已发布方案和指标的含义不可重写；有不兼容变更时应创建新版本。
- 方案和指标可以弃用，但必须保留已发布版本及其语义，说明替代项和迁移路径。

## 维护者审查清单

维护者审查指标、方案和目录变更时，至少确认：

1. JSON 格式和本地校验通过。
2. 方案、指标和目录之间的引用完整且版本表达清楚。
3. `supportedBy` 与当前 DSH runner 能力一致；未支持能力未被加入默认方案。
4. 指标目的、判定方式、适用范围和是否影响通过的描述清晰。
5. 规则足以让他人在相同版本下复现评测配置与结论。
