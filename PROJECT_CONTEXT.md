# 项目上下文

## 这个仓库是什么

这是一个由社区共同维护的 **DSH 插件评测数据集合**。它收集可版本化、可复用的插件评测包，帮助插件开发者在把插件接入 Agent 前完成基础评测。

它不是：

- 插件商店；
- 通用数据集平台；
- 评测执行平台、排行榜或结果仓库；
- 复杂的标准制定项目。

数据格式的作用只是让数据提供者、评测器和使用者对评测包如何编写和读取有共同预期。

## 用户与目标体验

第一目标用户是 DSH 插件开发者。理想流程是：

```text
写好插件 → 运行基础评测 → 获得安全性与兼容性报告 → 修复问题或接入 Agent
```

开发者不应手动挑选数据集、理解 profile/cases 或拼装测试环境。未来的评测 runner 应依据插件类型和权限声明，自动组合合适的检查包。

## 一次插件评测的基本元素

```text
被测插件 + 评测环境 + 评测用例 + 判断规则 + 结果报告
```

分工：

- **评测 runner**：加载插件、创建隔离环境、生成动态测试数据、观察行为、生成报告；
- **本仓库**：维护版本化的静态检查包，即测什么、哪些行为算通过；
- **插件开发者**：提供被测插件及必要的测试配置。

静态内容包括通用安全/兼容性规则、按插件类型划分的功能场景、公开测试资料和通过标准。动态内容包括当前插件版本、运行行为、随机测试值、mock 响应和报告。

## 当前数据模型

一个评测集目前由：

```text
profile：使用哪些指标、cases 的路径
cases：插件类型、用户输入与预期结果
fixtures（可选）：使预期结果可复现的准备数据
```

不要为了未来可能性提前设计复杂的通用格式。先做少量可运行、对开发者有实际价值的评测包，再按真实需求扩展。

## 参与方式

欢迎插件作者、使用者和领域专家参与：

1. 用 Issue 分享一个真实任务场景；
2. 提交小型 profile + cases 数据集；
3. 在外部仓库长期维护数据集，再加入本目录。

优先收录明确、可复现、能验证真实插件任务的场景。不得提交私有业务资料、个人信息、密钥或其他敏感内容。

## 已完成的站点与发现配置

- GitHub Pages：`https://dsh-plugin-evaluation.github.io/dsh-plugin-evaluation-standards/`
- GitHub 仓库 About Website 已指向该 Pages 地址；
- 网站构建会生成 `robots.txt` 和 `sitemap.xml`；
- Google Search Console 已完成站点所有权验证；
- Sitemap：`https://dsh-plugin-evaluation.github.io/dsh-plugin-evaluation-standards/sitemap.xml`。
