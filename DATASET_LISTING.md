# 数据集收录规范

本仓库是 DSH 插件评测数据集的 Store 目录。核心仓库不需要复制所有社区数据集；作者可以在自己的仓库维护 profile 和 cases，再向本目录提交收录条目。

## 收录要求

外部数据集的目录条目必须提供：

- 数据集名称、描述、插件类型和覆盖场景；
- 用例数量和使用的指标；
- GitHub 仓库地址；
- 固定 Git tag（例如 `v1.0.0`）或 40 位 commit SHA；
- 外部仓库内 profile JSON 的路径。

目录不接受 `main`、`master` 或其他浮动分支作为版本引用。

## 维护者检查

维护者在合并前检查：

1. 目录条目通过 `npm run validate` 和 `npm test`；
2. 仓库链接与固定版本引用完整；
3. profile 路径和描述与数据集实际内容相符；
4. 分类、场景和用例数量没有误导；
5. 没有 API Key、Token、密码、私有插件内容或未经许可的业务数据。

被收录表示数据集可被发现、版本已固定且目录信息经过核对；不表示数据集质量优秀、安全无风险或获得任何官方认证。

## 下架

当外部仓库失效、固定版本不可用、描述不再真实或发现敏感内容时，维护者可以移除目录条目。
